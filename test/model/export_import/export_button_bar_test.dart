void main() {
  /* TODO: make file picker testable and test
  testWidgets('should import DBs from 1.6.4 and earlier', (tester) async {
    await pumpApp(tester, await appBase(ExportButtonBar()));
    await tester.pumpAndSettle();
  });*/
}
